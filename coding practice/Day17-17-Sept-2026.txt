# Day 17 - Muti-Cloud Deployments, Auto-healing, and Advanced CI/CD
## Ansible - Auto-Healing Web Servers with Systemd

```yaml
- name: Configure Auto-healing for Web Server
  hosts: all
  become: yes
  tasks:
    - name: Ensurenginx service is running
      service:
        name: nginx
        state: started
        enabled: yes
    - name: Setup auto-restart for nginx service
      linenfile:
        path: /etc/systemd/system/nginx.service.d.override.conf
        line: "Restart=always"
        create: yes
    - name: Reload systemd
      command: systemctl deamon-reload


## Terraform - Multi-Cloud Infrastructure with AWS and Azure
#AWS VPC
resoruce "aws_vpc" "main" {
  cide_block = "10.0.0.0/16"
}


#Azure VNet
resource "azurerm_virtual_network" "main" {
  name                = "pathnex-vnet"
  location            = "East US"
  address_space       = ["10.0.0.0/16"]
  resoruce_group_name = azurem_resoruce_group.main.name
}

#EC2 instance in AWS
resoruce "aws_instance" "pathnex_ec2" {
  ami           = "ami-0abcs1234abcd1234"
  instance_type = "t2.micro"
  region        = "us-east-1"
}

#Virtual Machine in Azure
resoruce "azurem_linux_virtual_machine" "pathnex_vm" {
  name                = "pathnex-azure-vm"
  resoruce_group_name = azurerm_resource_group.main.location  location            = azurerm_resoruce_group.main.location  size                = "Standard_B1s"
  admin_username      = "azureuser"
  admin_password      = "P@ssw0rd123!"
}

## Kubernetes - Self-Healing with Liveness and Readiness Probes
apiVersion: app/v1
kind: Deployment
metadata: 
  name: pathnex-web-app
spec:
  replicas: 3
  selector:
    matchLabels:
      app: pathnex-web-app
  tamplate:
    metadata:
      labels:
        app: pathnex-web-app
    spec:
      containers:
        - name: nginx
          image: nginx
          ports:
            - containerPort: 80
          livenessProbe:
            httpGet:
              path: /healthz
              port: 80
              initialDelaySeconds: 5
              periodSeconds: 10
            readinessProbe:
              httpGet:
                path: /healthz
                port: 80
              initialDelaySeconds: 5
              periodSeconds: 10

# Jenkinsfile - Rolling Deployments with Kubernetes
pipeline {
    agent any
    enviroment {
      IMAGE_NAME ='pathnex-web-app'
    }
    stages {
      stage('Build') {
        steps {
          echo 'Building docker image...'
          docker.build( "${env.IMAGE_NAME}" )
        }
      }
      stage('Test') {
        steps {
          echo 'Running Tests..'
        }
      }
      stgae('Deploye to Kubernetes') {
        steps {
          script {
            sh 'kubectl apply -f kubernetes/deployment.yaml'
            sh 'kubectl rollout statur deployment/pathnex-web-app'
          }
        }
      }
    }
  }

# GitLab CI/CD - Multi-Cloud Deployment
stages:
  - build:
  - push:
  - deploy:

build:
  stgae: build
  script:
    - docker build -t pathnex-web-app .

push:
  stage: push
  script:
    - docker login -u "$CI_REGISTRY_USER" -p "$CI_REGISTRY_PASSWORD"
    - docker push pathnex-web-app

deploy_aws:
  stage: deploy
  script:
    - kubectl --context=azure-cluster apply -f kubernetes/deployment.yaml

## Doker
#MYSQL Container (Note - Here "\" means to be continue - it can be written in a single line")
docker run -d \
--name pathnex-mysql \
-e MYSQL_ROOT_PASSWORD=root \
mysql:8

#Real Path
/var/lib/mysql



        

